# Global VS Code extensions
# One extension ID per line — publisher.extension-name format
# Lines starting with # are comments and are ignored by deploy.sh
# See extensions.md for descriptions and documentation links
#
# CURATED FOR READING CODE, NOT WRITING IT (REC-O-187). Citizen 000 does not author code
# by hand any more, so the authoring aids this list used to carry -- completion, indent,
# docstring generation, format-on-save, inline type errors -- installed a cost with no
# reader on the other end. What survives serves three uses that are real: reading code
# someone else wrote, running python scripts, and converting markdown to PDF.
#
# NOT LISTED, ON PURPOSE: the AI assistant extension. Which one a machine gets is a
# per-machine decision recorded in config.env as DOTFILES_AI_EXTENSION, because a no-admin
# client machine may not be permitted Claude Code at all. 3_deploy installs it from there.

eamodio.gitlens
ms-python.debugpy
ms-python.python
ms-python.vscode-pylance
ms-python.vscode-python-envs
yzane.markdown-pdf
